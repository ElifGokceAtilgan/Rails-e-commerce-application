<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
     <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.1/dist/css/bootstrap.min.css" integrity="sha384-zCbKRCUGaJDkqS1kPbPd7TveP5iyJE0EjAuZQTgFLD2ylzuqKfdKlfG/eSrtxUkn" crossorigin="anonymous">
  </head>
  <body>
    <div class="container-fluid">
      <div class="row no-gutters pt-3">
          <div class="col-4 header-color justify-content">
            <h2>EGA SHOP</h2>
            <h6>For the best shopping!</h6>
          </div>
          <div class="menu bg-menu col-8">
            <div class="float-right">
            <ul class= "d-inline-flex list-unstyled">
               <img src ="https://imzalikitabim.com/wp-content/uploads/2018/02/S%C4%B0TE-%C4%B0CON-03-min.png" class= "img-icon">
               <li><%= link_to 'CART', carts_path %></li>
            </ul>
          </div>
        </div>
      </div>
      <div class="row box bg-b">
        <div class="col-4 justify-content">
          <h2>PRODUCTS</h2>
        </div>
        <div class="d-flex flex-wrap justify-content-center"> 
          <% @products.each do |product| %>
            <div class="col-12 col-sm-6 col-md-4 col-lg-3 mb-4">
              <div class="product-item">
                <img src="<%= product.image_url %>" alt="<%= product.name %>">
                <p><%= product.name %></p>
                <p>Price: <%= product.price %></p>
                <p>Stock: <%= product.stock %></p>
                <%= button_to 'ADD TO CART', add_to_cart_carts_path(product_id: product.id), method: :post, class: 'bg-a' %>
              </div>
            </div>
          <% end %> 
       </div>
  </body>
</html>